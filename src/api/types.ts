export interface FakePostItem {
	userId: number;
	id: number;
	title: string;
	body: string;
}

export interface FakeUser {
	image: string;
	email: string;
	id: string;
	name: string;
	admin: boolean;
}
